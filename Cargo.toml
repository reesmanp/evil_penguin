[package]
name = "evil_penguin"
version = "0.1.0"
authors = ["Paul Reesman <paul.reesman@nordstrom.com>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
amethyst = { version = "^0.12.0", features = ["metal", "nightly"] }
